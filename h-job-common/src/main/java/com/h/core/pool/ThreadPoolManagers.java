package com.h.core.pool;import java.util.concurrent.LinkedBlockingQueue;import java.util.concurrent.ThreadPoolExecutor;import java.util.concurrent.TimeUnit;/** * 公用线程池 */public class ThreadPoolManagers {//    private static int THREAD_NUM = Runtime.getRuntime().availableProcessors() * 2;    private static int CORE_THREAD_NUM = 40;    private static int MAX_THREAD_NUM= 100;    /**     * 队列长度     */    private static int CAPACITY=1000;    /**     * 线程池     */    private static ThreadPoolExecutor pool;    private static ThreadPoolManagers instance = new ThreadPoolManagers();    private ThreadPoolManagers() {        pool=new ThreadPoolExecutor(CORE_THREAD_NUM,MAX_THREAD_NUM,                3000L, TimeUnit.SECONDS,new LinkedBlockingQueue<>(CAPACITY));        pool.allowCoreThreadTimeOut(true);    }    public static ThreadPoolManagers getInstance() {        return instance;    }    public void execute(Runnable worker) {        pool.execute(worker);    }}